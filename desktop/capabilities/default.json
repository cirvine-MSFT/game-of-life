{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main visualizer window. Grants the core IPC + event surface so the React frontend can call our custom #[tauri::command] functions and subscribe to gol://* events. Filesystem and dialog permissions land with the file-ops todo.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit",
    "core:event:allow-emit-to",
    "core:window:default",
    "core:webview:default",
    "core:app:default",
    "core:path:default"
  ]
}
